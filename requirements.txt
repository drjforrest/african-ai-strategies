# Core data / numerics
numpy>=1.24
pandas>=2.0

# Embeddings (primary backend). Pulls in torch + transformers.
# If you cannot install this, the pipeline falls back to a TF-IDF backend
# (scikit-learn) so it still runs — see src/models.py.
sentence-transformers>=2.2

# TF-IDF fallback backend + clustering utilities
scikit-learn>=1.3
scipy>=1.10

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# PDF text extraction (used offline to convert source PDFs -> .txt; not
# imported by the runtime pipeline). Either works.
pdfplumber>=0.10
PyPDF2>=3.0

# Testing (optional, dev)
pytest>=7.4
